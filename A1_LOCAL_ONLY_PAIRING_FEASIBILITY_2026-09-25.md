# A1 去云端配对与密钥：可行性核查

日期：2026-09-25。核查对象为已有官方8.5.8.3 APK与历史设备研究记录。本轮没有向卡写入命令，没有修改密钥、解绑、复位或刷固件。

## 结论

可以移除独立客户端日常运行中的官方账号和云查询，前提是客户端已经持有这张卡认可的deviceSecret。不能据此承诺任意新卡在完全不接触官方服务的情况下完成首次接入。自行生成一把密钥只改变手机端，除非存在让卡安全接受新密钥的配置入口，否则不能替换设备鉴权。

## 本轮源码证据

| 问题 | 证据 | 可下结论 |
| --- | --- | --- |
| 日常连接是否必须在线计算token | classes19 `lj2.java:2569–2581` 从x78缓存取密钥；`:1185–1196` 用AESEncrypt处理设备随机值，再发307即0x0133 | 该段计算本地完成；这里未向账号服务器请求token |
| 手机端空密钥是否自动本地协商 | `lj2:2569–2576` 检查为空并直接报错返回 | 官方这个连接入口没有空密钥自动协商分支 |
| 密钥缓存是什么 | `x78.java:313–319`返回DeviceModel.deviceSecret | 读取已有密钥，不是生成新密钥 |
| 是否找到SDK设置密钥API | 核查42个JNI方法、DingerCommandHelper及dtiot_ble_request | 未找到明确的设置/轮换deviceSecret入口；不能由此证明固件不存在其他入口 |
| 通用首次绑定后向设备发送什么 | `BleBindPresenter.java:1776–1790`向云请求bindAndActive；`:1771–1796`取响应；`:934–938`把devId、activeCode、devUid送入BLE active | 官方通用路径包含服务器返回的激活资料，不能简单当作手机生成本地密钥 |
| 激活接口结构是否确实如此 | 本轮从base.apk/classes3.dex提取的 `BleInterface.java:12–44, 501–502` | ActiveReqModel有activeCode、devId、devUid、timeZone等；未出现具名deviceSecret。activeCode内部内容和设备验证规则未知 |

源码位于 `apks/8.5.8.3/work/src/sources/defpackage/` 与 `apks/8.5.8.3/work/binding/`。通用绑定分支不等于日版A1当前必经分支；日版实际分发路径的边界见 `A1_BINDING_KEY_FLOW_2026-09-25.md`。

## 历史实机证据，不冒充本轮重测

FINDINGS.md:303–315记录了两组真实random/token的计算匹配；:365–375记录独立客户端0x0008→本地token→0x0133得到code 200，并读取状态和下载文件。由此支持“已有凭据后脱离官方账号服务运行”。这些记录不能外推为新版本、新固件、任意未绑定卡的首次激活均已通过。

旧记录:317使用了“一次性绑定时由云端下发”的概括。更严格的说法是：已证实客户端通过设备资料查询取得密钥；密钥究竟由生产环节预置、服务端派生还是激活时写入，现有客户端证据不足以确定。

## 不同目标的可行性

1. **现有卡纯本地运行**：采用本地设备档案保存已取得的凭据，连接时本地完成鉴权；自定义动作及音频处理自行接管。无需在每次连接时保留官方账号查询。
2. **用户换手机或安装开源客户端**：可通过用户主动导入自己的凭据档案衔接。这解决凭据迁移，但不解决首次取得凭据。
3. **完全本地首次配对**：需要找到并验证未绑定设备的本地授权/密钥配置机制，或可由用户操作确认的设备端入口。目前没有证据确认此能力。
4. **自行生成新密钥或自己的服务器替代官方**：只有在卡接受新配置/激活资料时才有意义。模仿接口JSON和返回成功不会改变卡的验证结果。

## 继续研究的具体问题

- 查日版实际激活流程及既有激活抓包，区分activeCode与deviceSecret的关系；优先使用已有证据，不为抓包重置现有卡。
- 若客户端只把不透明激活资料转发给卡，需要设备端代码/固件或厂商协议说明，才能判断可否本地生成、导入或更换密钥。
- 已知challenge和应答不等于能计算未知密钥；复制官方加密函数也不会生成设备认可的秘密。不将穷举密钥列为实际接入方案。
- 复位命令存在不表示复位会清除出厂密钥、开放新密钥写入或绕过账号激活。未证实前不以复位作为解决办法。

当前最可落地的去云端范围是“本地保管现有凭据并离线连接”；剩余研究应集中在“首次凭据如何配置进设备/交给所有者”，而非重写音频或账号界面。
