# A1：以复用官方 SDK 为主线

接入目标：直接提取、内嵌并调用官方设备/音频模块。不以完全还原协议、重写全部底层算法为完成条件。左键自定义不在范围内。

## 当前已经存在的基础

- 独立App已包含ARM64的libDingerSdk.so及libc++_shared.so。
- `a1app/app/src/main/java/com/example/recorder/device/A1Native.kt` 已封装初始化、双向BLE字节通道、事件/音频回调、文件同步、容器转换、导出、合并和裁剪。
- `A1Session.kt:38` 将BLE原始通知交给native.feed；`:417–419`在对应条件下调用SDK的SendFileSyncCmd建立文件接收状态。
- ELF DT_NEEDED确认当前嵌入库和8.5.8.3研究库均依赖libc++_shared.so、liblog.so、libm.so、libdl.so、libc.so。这只是动态链接依赖，不能证明JNI、配置、模型资源等运行期依赖已经齐全。
- 当前嵌入库SHA-256：b36f14782679bbd086ad7ebe397fd37450786d25610917510ca5d39a9e1b58a6。
- 8.5.8.3研究库SHA-256：61572524ea708221e99fe8d1c7ab977b84c20050103b97a0fceb01db69fc2c41。两者不同，不直接把新版静态行为当成已打包版本行为。

## 必须掌握的边界

1. 同一版本的库、JNI声明、回调签名、初始化次序及必要配置/资源。
2. BLE字节输入输出的格式、队列和线程；协议ACK、同步状态、文件写入各只有一个管理者。
3. 认证需要的deviceSecret及初始化副作用；SDK本身不等于已取得用户账号授权或设备密钥。
4. SDK返回事件、音频回调的真实格式、错误和最终完成条件。不能仅凭回调命名认定PCM或收到stop即文件完整。
5. 启停、断连、取消、重连的生命周期；实际产物能够解码、播放和交给自有处理服务。

组合帧划分、补传、加密容器等内部算法，优先交由同版本SDK处理。只有外部行为异常或缺少必要接口证据时才继续下钻。之前组合包统计属于定位自写接收路径问题的证据，不再据此启动整套底层重写。

## 可运行形态

普通Android宿主 + 对应ARM64库 + 配套JNI包装 + 必要配置/文件目录/回调。现有证据不要求先建设虚拟机或把完整钉钉App装进容器。是否需要单独进程隔离属于后续稳定性选择，不能消除CPU ABI、Android运行库或账号权限依赖。

自有代码负责界面、设备连接、设置、任务状态、保存结果，以及ASR/翻译/总结/动作接口。SDK负责已封装的设备和音频底层流程。云端模型与服务不包含在本地.so里，需要接自有服务实现。

## 开源复现

开源适配层、构建配置、提取/校验说明与测试材料；官方二进制不是本项目原创源码。其分发许可未在本轮核实，发布包已单独收录核心组件，使用时按NOTICE核对第三方权利边界。此方式仍不能自动解决无root用户的密钥取得，凭据接入须单独验证。

本轮仅核对现有接线、依赖和确定工作方向；未更换库、修改App行为或声称设备集成已验收。
